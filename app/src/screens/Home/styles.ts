import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EEE',
  },
  titleWrapper: {
    display: 'flex',
  },
  title: {
    fontSize: 30,
    fontWeight: 'bold',
  },
});

export default styles;
